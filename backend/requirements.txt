# Target Python 3.11 (TensorFlow / MediaPipe do not ship 3.13 wheels yet).
#   py -3.11 -m venv .venv  &&  .venv\Scripts\activate  &&  pip install -r requirements.txt

fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
sqlmodel==0.0.22
psycopg[binary]==3.2.3
python-multipart==0.0.20

numpy==1.26.4
opencv-python-headless==4.10.0.84
mediapipe==0.10.18
# Keep yt-dlp current — YouTube changes frequently break older versions.
# Recent yt-dlp also needs a JavaScript runtime to extract YouTube formats;
# Node (already required for the frontend) is used automatically, or install Deno.
yt-dlp>=2026.7.4

# Optional learned layer. Everything except the LSTM autoencoder works without it.
tensorflow==2.18.0
